# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
/work/
/.worktrees/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.sandbox-*.log
firestore-debug.log
sandbox-server*.log

# env files (can opt-in for committing if needed)
.env*
!.env.example
/env.local

# vercel
.vercel
/.superpowers/

# typescript
*.tsbuildinfo
next-env.d.ts

# firebase / credentials
*-adminsdk-*.json

# scratch output from scripts/find-plant-images.mjs — downloaded candidates
# awaiting human review/caption, not final content. Also used to hold private
# reference copies of copyrighted images per newplant_protocol.md stage 2 —
# this repo is PUBLIC on GitHub, so nothing here may ever be committed.
/docs/superpowers/image-candidates/

