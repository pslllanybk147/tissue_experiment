# Plantlover Lab — สรุปโครงสร้างระบบทั้งหมด

> อัปเดต 7 สิงหาคม 2026 · เทสต์ 488 ผ่าน 10 skip · lint สะอาด · build ผ่าน
>
> เอกสารนี้อธิบาย **โครงสร้างของระบบตั้งแต่ต้นจนจบ** ว่ามีอะไรบ้าง แต่ละส่วนทำหน้าที่อะไร
> และเชื่อมกันอย่างไร ถ้าอยากรู้ว่า **ทำไม** ถึงออกมาเป็นแบบนี้ อ่าน `docs/superpowers/redesign-record.md`

---

## 1 · ระบบนี้คืออะไร

เว็บคู่มือเพาะเลี้ยงเนื้อเยื่อพืชแบบทีละขั้น ที่ **บอกระดับความน่าเชื่อถือของทุกคำแนะนำ**
และคำนวณปริมาณสารให้ทำได้จริงด้วยอุปกรณ์ที่ผู้ใช้มี

หลักการที่คุมทั้งระบบ

1. **อ่านก่อนได้ ลงมือทีหลัง** คู่มืออ่านครบได้โดยไม่ต้องล็อกอิน
2. **คู่มือบอกว่าต้องได้อะไร ระบบบอกว่าด้วยของที่มีจะได้มายังไง**
3. **ทุกคำแนะนำต้องบอกได้ว่ามาจากไหน** และถ้าไม่มีที่มา ต้องบอกว่าค้นอะไรไปแล้ว
4. **ห้ามแสดงตัวเลขที่ทำตามไม่ได้** ถ้าชั่งไม่ได้ต้องบอกตรง ๆ แล้วเสนอทางแก้

---

## 2 · แผนผังเส้นทาง สามโซน

### โซนสาธารณะ · ไม่ต้องล็อกอิน

| เส้นทาง | หน้าที่ |
|---|---|
| `/` | สี่ประตูตามสถานะตั้งต้นของผู้ใช้ |
| `/find` | ไล่คำถามจากลักษณะต้นเพื่อหาทรง ขับด้วย query string |
| `/form/<formId>` | หน้าทรง จุดสังเกตที่ต้องหา และตำแหน่งตัด |
| `/search?q=` | ค้นชนิด สกุล และทรง พร้อมทางออกเมื่อค้นไม่เจอ |
| `/start` | ทรงเรียงตามความยากสำหรับมือใหม่ |
| `/problem?symptom=` | เข้าจากอาการที่เห็น |
| `/guide/<slug>` | แผนที่ 14 ขั้นของพืชนั้น พร้อมระดับหลักฐานรายขั้น |
| `/guide/<slug>/step/<n>` | เนื้อหาขั้นเดียว ภาพประกอบ เกณฑ์ผ่าน จุดที่ต้องหยุด อาการที่อาจเจอ พร้อมคำศัพท์ที่แตะดูความหมายได้ |

หน้าเหล่านี้เป็น Server Component ล้วน prerender ตอน build อ่านจากไฟล์ในโค้ด ไม่แตะฐานข้อมูล

### โซนผู้ใช้ · ต้องล็อกอิน

| เส้นทาง | หน้าที่ |
|---|---|
| `/my` | หน้าสรุปหลังบ้าน |
| `/my/rounds` | รายการรอบเพาะ พร้อมความคืบหน้า และรอบเก่าที่แยกไว้ |
| `/my/rounds/new?slug=<slug>` | สร้างรอบจากคู่มือแล้วพาไปหน้ารอบ |
| `/my/rounds/<id>` | ภาพรวมทั้ง 14 ขั้น ไฮไลต์ว่าทำต่อตรงไหน |
| `/my/rounds/<id>/step/<n>` | ตัวเดินขั้นตอน บันทึกค่า จดข้อความ แนบรูป |
| `/my/rounds/legacy/<id>` | อ่านรอบที่สร้างก่อนระบบคู่มือใหม่ แบบอ่านอย่างเดียว |
| `/my/equipment` | บอกของที่มี แล้วระบบจัดเส้นทางให้ |

### โซนหลังบ้าน

| เส้นทาง | หน้าที่ |
|---|---|
| `/admin/manual` และ `/admin/manual/<slug>` | กางคู่มือฉบับประกอบเสร็จเพื่อตรวจทาน |
| `/admin/knowledge` | ทะเบียน taxon แหล่งอ้างอิง และ claim ที่รออนุมัติ |
| `/admin/research` | ตรวจงานวิจัย |
| `/admin/dataset-review` | ตรวจรูปสำหรับงาน ML |
| `/admin/pin` | ปักหมุดจุดสังเกตบนภาพของทรง แล้วคัดลอกพิกัดไปวางในไฟล์ทรง |

---

## 3 · โครงเนื้อหาคู่มือ · `src/lib/manual/`

**แกนกลางเป็นคลังขั้นตอน ไม่ใช่ลำดับตายตัว**

```
types.ts          สคีมา ManualStepDef, PlantPack, EvidenceRef, MediaRecipe, TroubleshootingEntry
core-steps.ts     คลังขั้นตอนกลาง 14 ขั้น ใช้ร่วมกันทุกพืช
troubleshooting.ts คลังอาการกลาง เช่น การดำจากฟีนอลิก กับการดำจากฟอกแรงเกิน
sources.ts        ทะเบียนแหล่งอ้างอิงพร้อมใบอนุญาต
resolve.ts        ประกอบทั้งสี่ชั้นเป็นคู่มือฉบับสมบูรณ์
registry.ts       รวมแผ่นเสริมทั้งหมด และ resolve ตาม slug
summary.ts        นับสถิติของคู่มือ ใช้ในหน้าตรวจทาน
evidence-level.ts กฎจุดอ่อนที่สุดของคู่มือ
terms.ts          แยกคำศัพท์ที่ห่อด้วย [[id|ข้อความ]] ออกจากข้อความ
traits.ts         ลักษณะพันธุ์ที่ตัดข้ามสกุล เช่นความด่าง
forms/            ชั้นทรงการเติบโต คำศัพท์และจุดสังเกต ใช้ร่วมกันข้ามสกุล
genera/           ชั้นสกุล เก็บเฉพาะส่วนที่ต่างจากทรง
species/          แผ่นเสริมรายชนิด หนึ่งไฟล์ต่อหนึ่งพืช
```

**การประกอบทับค่าจากบนลงล่างสี่ชั้น `core → form → genus → species`**
ชั้นล่างชนะเสมอ ฟิลด์ที่ชั้นล่างไม่พูดถึงจะตกทอดจากชั้นบนมาเอง และ `origin` ของแต่ละขั้น
บอกชั้นล่างสุดที่แตะขั้นนั้น ยกเว้นขั้นที่แผ่นเสริมเขียนเองทั้งขั้น (`pack.steps`)
ซึ่งไม่รับการทับจากทรงหรือสกุล เพราะการเขียนขั้นใหม่แปลว่าตั้งใจไม่ใช้ของกลางแล้ว

แผ่นเสริมรายชนิดมีสี่ส่วน

- `sequence` เลือกและเรียงขั้นเอง **ถอดขั้นที่ไม่เกี่ยวออกได้**
- `overrides` ทับค่าหรือข้อความของขั้นจากชั้นบน
- `steps` **เขียนขั้นใหม่ที่แกนกลางไม่มีได้เต็มขั้น**
- `mediaRecipes` สูตรอาหารของพืชนั้น

และผูกขึ้นชั้นบนด้วย `growthFormId`, `genusId`, `traitIds`

ผลคือพืชที่วิธีต่างกันสิ้นเชิง เช่น Anthurium ที่ขยายผ่าน callus เขียนได้ในไฟล์เดียว
โดยไม่ต้องแตะแกนกลาง และขั้นที่เหมือนกันจริง ๆ ยังใช้ต้นฉบับร่วมกัน แก้ที่เดียวได้ทุกพืช

**ทำไมถึงมีชั้นทรงและชั้นสกุล** เพราะสิ่งที่มือใหม่ต้องรู้เพื่อลงมือ (ข้ออยู่ตรงไหน ตัดตรงไหน)
ไม่แปรตามสายพันธุ์แต่แปรตามทรง และโปรโตคอลเกาะกลุ่มตามสกุลมากกว่าตามชนิด
ชั้นสกุลจึงทำงานหนักที่สุดในเชิงความครอบคลุม ส่วนความด่างไม่ใช่ของสกุลใดสกุลหนึ่ง
จึงเป็น `Trait` แยกแกน ไม่ใช่ชั้นใน cascade
เหตุผลเต็มอยู่ใน `docs/superpowers/specs/2026-08-05-growth-form-first-redesign-design.md`

**พืชที่มีคู่มือเฉพาะชนิด** Pink Princess, Violin variegated, เส้นทางกลางของ Philodendron ที่ยังไม่ยืนยันชนิด
และ Monstera deliciosa ‘Thai Constellation’ ซึ่งเป็นชนิดแรกที่ผ่านการค้นหลักฐาน 11 ช่องทางครบ
และเป็นชนิดแรกที่มีค่าช่วงสำหรับขั้นทดสอบช่วง

**ทรงที่มีแล้ว ครบแปดทรง** เรียงตามความยากสำหรับมือใหม่

| ทรง | ยาก | หลักฐานของตำแหน่งตัด |
|---|---|---|
| `climbing-vine-visible-node` | 1 | ยังไม่มีงานรองรับ |
| `rhizome-bud` | 2 | ประยุกต์ |
| `thick-leaf-no-stem` | 2 | ประยุกต์ |
| `leaf-vein-bud` | 2 | ประยุกต์ |
| `culm-node` | 2 | ประยุกต์ · **ทรงเดียวที่งานอ้างอิงเริ่มจากต้นในแปลงจริง** |
| `rosette-sheathed-node` | 3 | ประยุกต์ |
| `pseudobulb-node` | 3 | ยังไม่มีงานรองรับ |
| `woody-shrub-node` | 3 | ประยุกต์ · สองแหล่งขัดกัน เก็บไว้ทั้งคู่ |

**สกุลที่มีแล้ว สิบสามสกุล ครบทั้งแปดทรง** Philodendron, Monstera, Epipremnum, Syngonium,
Anthurium, Alocasia, Sansevieria, Begonia, Zingiber, Musa, Dendrobium, Adenium, Bambusa

ทุกทรงมีสกุลอย่างน้อยหนึ่งสกุลแล้ว ยกเว้น Philodendron ที่ยังเป็นโครงเปล่า
เพราะเนื้อหาของมันอยู่ที่ชั้นชนิดทั้งสามไฟล์อยู่แล้ว

**บทเรียนจากการเขียนเนื้อหารอบนี้ที่ควรจำ**

1. **มีหลักฐานไม่ได้แปลว่าทำตามได้** Anthurium มีงานวิจัยมากที่สุดในบรรดาสกุลที่เขียน
   แต่เกือบทุกงานได้ผลดีที่สุดด้วยเมอร์คิวริกคลอไรด์ ซึ่งเป็นสารปรอทที่ใช้และทิ้งที่บ้านไม่ได้
   ขั้นฟอกของสกุลนี้จึงติดป้ายว่ายังไม่มีงานรองรับ **สำหรับวิธีที่ทำตามได้จริง**
   ดีกว่าพิมพ์ตัวเลขที่ห้ามผู้อ่านทำตาม
2. **งานส่วนใหญ่ของกล้วยไม้เริ่มจากเนื้อเยื่อที่ปลอดเชื้ออยู่แล้ว** ตามเกณฑ์คัดกรองข้อหนึ่ง
   แปลว่าขั้นตัดและฟอกจากต้นแม่จริงไม่มีหลักฐาน ทรง `pseudobulb-node` จึงเป็น `unsupported`
3. **สองแหล่งขัดกันให้เก็บทั้งคู่** กุหลาบใช้ชิ้นข้อได้ดี แต่ Adenium ชิ้นข้อดำภายในสองสัปดาห์
   และยอดอ่อนดีกว่า ทั้งคู่อยู่ใน `woody-shrub-node` พร้อมคำแนะนำว่าถ้าต้นมียางมากให้เริ่มจากยอดอ่อน
4. **สารปรอทเป็นปัญหาเชิงระบบ ไม่ใช่ของ Anthurium สกุลเดียว** Bambusa, Zingiber และ Dendrobium
   ต่างก็ให้ผลดีที่สุดด้วยเมอร์คิวริกคลอไรด์เหมือนกัน แปลว่าช่องว่างนี้เป็นช่องว่างของวรรณกรรมทั้งวงการ
   ไม่ใช่ของพืชชนิดใดชนิดหนึ่ง และเป็นเหตุผลที่ขั้นทดสอบช่วงมีค่ามาก
5. **แต่ก็ไม่ใช่ทุกสกุลที่ตัน** งาน Dendrobium ปี 2024 เทียบสารฟอกจากต้นแม่จริงแล้วพบว่า
   น้ำยาซักผ้าขาวตั้งต้นได้ 67.2% เทียบกับสารปรอทที่ 69.6% ต่างกันสองจุดกว่า
   แลกกับชิ้นตาย 20.8% เทียบกับศูนย์ **การรู้ราคาที่ต้องจ่ายมีค่ากว่าการรู้ว่าทำไม่ได้**

---

## 4 · ระบบหลักฐาน

สี่ระดับ `species-direct` ตรงพันธุ์ · `adapted` ประยุกต์ · `unsupported` ยังไม่มีงานรองรับ
· `botanical-fact` ข้อมูลจากตำรา

**สามระดับแรกใช้กับ "ข้ออ้าง"** คือข้อความที่สั่งให้ลงมือทำหรือมีตัวเลขที่ต้องทำตาม
**ส่วน `botanical-fact` ใช้กับ "คำนิยาม"** เช่น "ข้อคือวงนูนที่ใบและรากงอกออกมา"

วิธีแยกสองแบบนี้คือถามว่า **ถ้ามันผิด เรารู้ตอนไหน** คำนิยามเปิดตำราเทียบก็รู้เดี๋ยวนั้น
ส่วนข้ออ้างต้องลงมือทำแล้วรอสามสัปดาห์ ถ้าประโยคไหนสั่งให้ทำอะไร หรือมีตัวเลขที่ต้องทำตาม
ประโยคนั้นเป็นข้ออ้าง ประโยคที่ปนกันสองแบบต้องแยก เช่น "ให้ใช้ข้อ อย่าใช้ปล้อง เพราะปล้องไม่มีตา"
แยกเป็นคำนิยาม ("ปล้องไม่มีตา") กับข้ออ้าง ("ให้ใช้ข้อในการเพาะ")

**กฎที่บังคับด้วยเทสต์ ห้ามแก้เทสต์เพื่อให้ผ่าน**

| กฎ | ไฟล์ที่บังคับ |
|---|---|
| ข้ออ้างที่ว่ามีงานรองรับ ต้องระบุ `sourceIds` | `evidence-rules.test.ts` |
| ข้ออ้างที่ว่าไม่มีงานรองรับ ต้องบันทึก `searchQueries` และ `searchedAt` | `evidence-rules.test.ts` |
| ข้อมูลจากตำราต้องระบุ `sourceIds` ห้ามใช้เป็นทางเลี่ยงการอ้างอิง | `evidence-rules.test.ts` |
| ทุก `sourceId` ที่อ้าง ต้องมีอยู่จริงในทะเบียน | `sources.test.ts` |
| ทุก `troubleshootingId` ที่อ้าง ต้องมีอยู่จริง | `core-steps.test.ts` |
| ทุก `illustrationId` ที่อ้าง ต้องมีภาพจริง | `illustrations.test.tsx` |
| ทุก `growthFormId` และ `genusId` ที่อ้าง ต้องมีอยู่จริง | `genera/registry.test.ts` |
| จุดตัดต้องอ้าง landmark ที่มีอยู่ในทรงนั้น | `forms/registry.test.ts` |
| มีพิกัด `point` ได้เมื่อทรงมี `referenceImage` เท่านั้น | `forms/registry.test.ts` |
| ทุกไฟล์ภาพที่อ้าง ต้องมีอยู่จริงใน `public/forms/` | `forms/images.test.ts` |
| ภาพต้องบอกชนิดในภาพ คนถ่าย และใบอนุญาต CC BY-SA 4.0 | `forms/images.test.ts` |
| `alt` ของภาพต้องยาวเกิน 20 ตัวอักษร | `forms/images.test.ts` |
| ภาพต้องไม่กว้างเกิน 1400 px และไฟล์ไม่เกิน 250 KB | `forms/images.test.ts` |
| พิกัดหมุดต้องอยู่ในช่วง 0 ถึง 1 | `forms/images.test.ts` |
| `cropStyle` ต้องวางจุดไว้กลางช่องซูมจริง รวมถึงจุดริมภาพ | `forms/crop.test.ts` |
| คำนิยามของจุดสังเกตต้องเป็น `botanical-fact` และระบุแหล่ง | `forms/registry.test.ts` |
| `Dose` ต้องระบุ `form` และช่วงต้องเรียงถูกทาง | `forms/registry.test.ts` |
| ค่าระดับ `adapted` ในชั้นสกุล ต้องเขียน `note` ว่ายืมมาจากชนิดไหน | `genera/registry.test.ts` |
| ทุกขั้นที่สกุลทับค่า ต้องเป็นขั้นที่มีอยู่จริงในแกนกลาง | `genera/registry.test.ts` |
| ทุกสกุลต้องมีชื่อเรียกทั่วไปอย่างน้อยหนึ่งชื่อ ไม่งั้นคนไทยค้นไม่เจอ | `genera/registry.test.ts` |
| ทุกทรงในทะเบียนต้องมีเส้นทางใน `/find` เดินมาถึงได้ | `forms/finder.test.ts` |
| คำที่ห่อไว้ในเนื้อหาต้องมีอยู่จริงในทะเบียนคำศัพท์ | `term-integrity.test.ts` |
| ห้ามมีวงเล็บห่อคำที่เขียนรูปแบบผิดหลงเหลืออยู่ | `term-integrity.test.ts` |
| คำอธิบายจุดสังเกตห้ามห่อศัพท์ซ้อนเข้าไปอีก | `term-integrity.test.ts` |
| ค่าช่วงที่ให้มาระดับขั้น ต้องไม่ถูกค่าระดับทรงหรือสกุลกลืนหาย | `resolve.test.ts` |
| ค่าช่วงต้องเรียงตาม cascade คือแกนกลางอ่อนสุด ชนิดพืชแก่สุด | `resolve.test.ts` |
| ขั้นที่มีหลักฐานตรงชนิด ต้องไม่ถูกสั่งให้แบ่งชุดทดสอบ | `bracket.test.ts` |
| ค่าที่ผู้ใช้ทดสอบเองห้ามยกระดับป้ายหลักฐานของขั้นนั้น | `bracket.test.ts` |
| ช่องกรอกของตารางทดสอบช่วงต้องอยู่ใน `<form>` เดียวกับปุ่มบันทึก | `step-runner.test.tsx` |
| ปักหมุดรัวในจังหวะเดียวกัน ต้องได้ครบทุกจุดและไม่สลับตัว | `pin-picker-logic.test.ts` |
| ชั้นสกุลอ้างระดับตรงพันธุ์ไม่ได้ ทั้งในขั้นและในค่าช่วง | `genera/registry.test.ts` |

กฎข้อสองคือหัวใจ **การบอกว่า "ไม่มีงานวิจัยรองรับ" ต้องมีหลักฐานของตัวเองเหมือนกัน**
เพื่อให้ตรวจย้อนได้ว่าช่องว่างนั้นจริง หรือแค่ค้นไม่พอ และรู้ว่าควรกลับไปค้นใหม่เมื่อไหร่

**กฎจุดอ่อนที่สุด** ระดับรวมเท่ากับจุดที่อ่อนที่สุด ไม่ใช่จุดที่แข็งที่สุด
อยู่ที่ `evidence-level.ts` สำหรับคู่มือ และ `equipment/resolve-path.ts` สำหรับเส้นทางอุปกรณ์
ฝั่งคู่มือ **นับเฉพาะข้ออ้าง ไม่นับ `botanical-fact`** เพราะกฎนี้มีไว้เตือนความเสี่ยงที่ต้องรอผล
ถ้านับคำนิยามเข้าไปด้วย ทุกคู่มือจะถูกลากลงเท่ากันหมดและป้ายจะแยกเล่มไม่ออกอีกต่อไป
คืน `null` เมื่อยังไม่มีข้ออ้างให้ตัดสิน

**กฎเนื้อหาสำหรับมือใหม่ (ยังไม่มีเทสต์บังคับ ตรวจด้วยคนตอนรีวิว เพิ่ม 4 สิงหาคม 2026
หลังพบว่าขั้นตัด/ฟอกของ Violin ด่างมีจุดที่มือใหม่ตามทำไม่ได้จริง)**

1. **ทุกครั้งที่พูดถึงสารเคมีหรือสารใด ๆ ในเนื้อหา ห้ามเอ่ยแค่ชื่อสารลอย ๆ** ต้องระบุครบ
   - ชื่อสารและชนิด/รูปแบบที่ใช้จริง (เช่น ผง สารละลายสำเร็จรูป น้ำยาฟอกขาวเชิงพาณิชย์)
   - ปริมาณที่ใช้ และถ้าผสมน้ำ ต้องบอก**อัตราส่วนต่อปริมาตร** (เช่น ต่อลิตร) กับ**ความเข้มข้นที่ได้จริง**
     ไม่ใช่แค่ "1%" ลอย ๆ (ดูตัวอย่างความกำกวมของหน่วยในส่วนที่ 2 ข้อสาม)
   - ระยะเวลาที่ใช้หรือแช่
   - หาซื้อ/หาได้จากไหน โดยเฉพาะของที่หาซื้อตามบ้านไม่ได้ ต้องบอกทางเลือกที่ใช้แทนได้
   ตัวอย่างที่แก้แล้ว: "สารต้านออกซิเดชัน" เดิมไม่ระบุชื่อ แก้เป็น "สารละลายกรดแอสคอร์บิก (วิตามินซี)
   หรือกรดซิตริก" ใน `troubleshooting.ts`
2. **ทุกครั้งที่ทำภาพประกอบที่เกี่ยวกับการตัด ต้องระบุตำแหน่งตัดให้แม่นยำ** ทั้งในภาพ (จุดตัด/ข้อ
   ต้องอยู่บนโครงสร้างของพืชจริง ไม่ใช่อยู่บนองค์ประกอบตกแต่งอื่นในภาพ) และในข้อความกำกับ (ระยะเป็น ซม.
   หรือ มม. นับจากจุดอ้างอิงที่ชัดเจน เช่น "จากปลายยอด" ไม่ใช่แค่มีสัญลักษณ์กรรไกรอยู่ใกล้ ๆ) —
   ดูเคสตัวอย่างที่แก้แล้วใน `illustrations.tsx` (`cut-explant-violin-bipennifolium`) และ
   actions ของขั้น `cut` ใน `violin-variegated.ts`
3. **ห้ามใช้ศัพท์เทคนิคที่ยังไม่มีในทะเบียนคำศัพท์** (เพิ่ม 5 สิงหาคม 2026 พร้อมชั้นทรง)
   ทะเบียนคำศัพท์คือ `landmarks` ของทุกทรงใน `forms/` ไม่มีคลังคำแยกต่างหาก
   ถ้าจะใช้ศัพท์ใหม่ ต้องเพิ่ม landmark ในทรงที่เกี่ยวข้องพร้อม `howToFind` ก่อน
   แล้วห่อคำในเนื้อหาเป็น `[[landmarkId|ข้อความ]]`
   - **ต้องห่อด้วยมือ** เพราะภาษาไทยไม่มีช่องว่างคั่นคำ การจับอัตโนมัติจะทำให้ "ข้อ"
     ไปโดน "ข้อมูล" และ "ข้อควรระวัง"
   - **ตรวจจุดที่อาจลืมห่อด้วย `npm run terms:report`** ซึ่งเป็นรายงานให้คนอ่าน ไม่ใช่เกตอัตโนมัติ
     ด้วยเหตุผลเดียวกัน สิ่งที่บังคับด้วยเทสต์คือทิศทางกลับกัน (ดูสามแถวล่างสุดของตารางในส่วนที่ 4)
   - **คำอธิบายของ landmark ห้ามใช้ศัพท์ที่ต้องเปิดหาต่อ** ไม่งั้นมือใหม่จะวนหาความหมายไม่จบ

---

## 5 · รอบเพาะ · `src/lib/rounds/`

**ไม่มี collection ใหม่** รอบเพาะแมปลงบนโครงข้อมูลเดิม จึงไม่ต้อง migrate และไม่ต้องแก้ security rules

| แนวคิดใหม่ | เก็บเป็น |
|---|---|
| รอบเพาะ | `ExperimentLot` โดย `protocolId` เก็บ slug ของคู่มือ |
| บันทึกรายขั้น | `ProtocolStepRun` โดย `stepId` เก็บ id ของขั้นจากแกนกลาง |
| รูปหลักฐานของขั้น | `Observation` ชนิด `protocol-step-evidence` หนึ่งรายการต่อขั้น ทำหน้าที่แขวนรูป |

```
round-adapter.ts   ประกอบมุมมองรอบจาก lot + step runs + คู่มือ
legacy-rounds.ts   แยกรอบเก่าที่ resolve ไม่ได้ออกมา ไม่ให้หายไปเงียบ ๆ
step-evidence.ts   สร้างและค้น observation ที่ใช้แขวนรูป
medium-plan.ts     คำนวณสูตรอาหารตามจำนวนกระปุกและความละเอียดอุปกรณ์
```

**บันทึกที่อ้างขั้นซึ่งคู่มือไม่มีแล้วจะถูกข้าม ไม่ใช่โยน error** เพื่อให้แก้ลำดับขั้นของคู่มือ
ภายหลังได้โดยไม่ทำให้รอบเก่าเปิดไม่ได้

---

## 6 · เส้นทางอุปกรณ์ · `src/lib/equipment/`

กระบวนการต้องการสี่อย่าง **อาหารปลอดเชื้อ · น้ำปลอดเชื้อ · ภาชนะปลอดเชื้อ · ผิวชิ้นพืชสะอาด**

`capabilities.ts` เก็บวิธีที่ให้แต่ละอย่าง พร้อมอุปกรณ์ที่ต้องใช้และระดับหลักฐานของวิธีนั้น
`resolve-path.ts` จับคู่กับของที่ผู้ใช้มี แล้วเลือกวิธีที่หลักฐานดีที่สุดที่ทำได้

**ถามเป็นของที่หาซื้อได้จริง** มีตัวเลือกหม้ออัดแรงดันทำอาหารแยกจากหม้อนึ่งของแล็บ
เพราะที่ 15 psi ได้ 121 องศาเท่ากัน ถ้าถามด้วยคำว่า autoclave คนทำที่บ้านจะติ๊กว่าไม่มี
ทั้งที่มีของใช้แทนได้อยู่ในครัว

**เมื่อทำอย่างใดไม่ได้เลย ระบบกางทางเลือกพร้อมความเสี่ยงและไม่เลือกให้อัตโนมัติ**

ชุดอุปกรณ์เก็บที่ `users/{uid}/settings/equipment` ซึ่ง security rules เดิมอนุญาตอยู่แล้ว

---

## 7 · เครื่องคำนวณสูตรอาหาร

ปัญหาของงานเล็กไม่ใช่การคูณเลข แต่คือ **ปริมาณเล็กจนชั่งไม่ได้**

ลำดับการคิด

1. จำนวนกระปุก บวกกระปุกเปล่าคุมและสำรอง คูณปริมาตรต่อกระปุก บวกเผื่อสูญเสีย
2. คูณสูตรตามปริมาตรที่ได้
3. เทียบทุกบรรทัดกับความละเอียดของเครื่องชั่งที่ผู้ใช้ระบุ
4. บรรทัดที่ชั่งไม่ได้ **ห้ามปัดเศษ** ให้เรียก `calculateWorkingStock` เขียนวิธีทำน้ำยาแม่แทน
   โดย **เริ่มจากการชั่งผง** ที่มวลสูงกว่าขีดต่ำสุดของเครื่องชั่งห้าเท่า ไม่ใช่พอดีเป๊ะ

โมดูลคำนวณที่ยังใช้อยู่ `medium-batch-calculations`, `working-stock-calculator`, `haiter-calculations`

ทั้งสามตัวมี UI เรียกใช้ได้จริงแล้วผ่านเมนู "เครื่องคำนวณ" (ดูส่วนที่ 14) ก่อนหน้านี้มีแค่สูตรอาหาร
ที่ฝังอยู่ในขั้น `prep-media` เท่านั้น อีกสองตัวเป็นฟังก์ชันบริสุทธิ์ที่ทดสอบไว้แต่ไม่มีใครเรียกใช้

---

## 8 · ชั้นข้อมูล

ทุกอย่างอ่านเขียนผ่าน **repository interface** ห้ามเรียก Firestore SDK จาก component

แต่ละ repository มีสามส่วน คือ interface, ตัวจำในหน่วยความจำสำหรับโหมดสาธิต
ที่เขียนผ่าน `demo-storage` ลง localStorage, และตัว Firestore สำหรับผู้ใช้จริง
โดย factory เลือกให้ตามสถานะการล็อกอิน

repository ที่มี `experiment`, `step-run`, `media`, `equipment`, `dataset`,
`knowledge-library`, `knowledge-source`, `lab`

**ออฟไลน์** Firestore เปิด `persistentLocalCache` ไว้ จดข้อความและตัวเลขได้ตอนเน็ตหลุด
แล้วซิงก์เองเมื่อกลับมา **แนบรูปไม่ได้** และระบบอธิบายเหตุผลแทนการซ่อนปุ่มเงียบ ๆ

ข้อมูลทั้งหมดอยู่ใต้ `users/{uid}` และ security rules บังคับว่า `ownerId` ต้องตรงกับเจ้าของ

---

## 9 · ชั้นดีไซน์

ภาษา **Illustrated Step Book** เส้นขอบหนา เงาทึบไม่เบลอ สีสด ภาพประกอบทุกขั้น

`src/app/guide.css` เก็บ token ขึ้นต้น `--pl-` และ class ขึ้นต้น `pl-`
**แยกจาก `globals.css` เดิมทั้งหมด** เพื่อไม่ให้ชนกัน

**โหมดมืดออกแบบที่ระดับ token ตั้งแต่ต้น ไม่ใช่ invert ทีหลัง** ตอบสนองทั้ง
`prefers-color-scheme` และปุ่มสลับ โดยปุ่มชนะ media query ทั้งสองทาง
ธีมถูกตั้งด้วย inline script ก่อน hydrate เพื่อไม่ให้หน้ากระพริบ

ภาพประกอบ 15 ภาพใน `illustrations.tsx` เป็น SVG ที่ **ใช้สีจาก token ทั้งหมด**
จึงเปลี่ยนตามโหมดได้จริง

---

## 10 · แนวทางเทสต์

- เทสต์ component ใช้ `renderToStaticMarkup` ไม่มี jsdom ไม่มี testing-library
- component ที่แสดงผลเป็น sync ทั้งหมด ส่วน async อยู่ที่ `page.tsx` เท่านั้น
  เพื่อให้เทสต์ได้โดยไม่ต้อง mock
- ตรรกะคำนวณเป็นฟังก์ชันบริสุทธิ์ แยกจาก React
- `npm run ui:verify` ตรวจ 14 ความกว้างตั้งแต่ 360 ถึง 1920 px บน production build

**ข้อจำกัดที่ต้องรู้** วิธีนี้จับพฤติกรรมข้ามหน้าและข้ามการโหลดใหม่ไม่ได้
ข้อบกพร่องหลายข้อในโปรเจกต์นี้จึงเจอจากการเปิดดูของจริงเท่านั้น รายละเอียดอยู่ในส่วนที่ 6
ของ `docs/superpowers/newplant_protocol.md`

---

## 11 · วิธีเพิ่มพืชชนิดใหม่

ทำตามเก้าขั้นใน `docs/superpowers/newplant_protocol.md` (มาแทน `manual-authoring-protocol.md`
เดิม ซึ่งตอนนี้เหลือแค่ตัวชี้มาที่ไฟล์นี้) โดยย่อคือ

1. ยืนยันชื่อวิทยาศาสตร์จาก POWO หรือ WCVP
2. ค้นหลักฐานให้ครบสิบเอ็ดช่องทาง **รวมชื่อพ้อง ชื่อพันธุ์พี่น้อง วิทยานิพนธ์ สิทธิบัตร
   และฟอรัมนักปลูก (ระดับ anecdotal เท่านั้น)**
3. คัดกรองแปดข้อ โดยข้อสำคัญที่สุดยังคงเป็น **ตรวจว่างานนั้นเริ่มจากต้นแม่จริงหรือจากเนื้อเยื่อ
   ที่ปลอดเชื้ออยู่แล้ว** เพิ่มเติมคือตรวจขนาดตัวอย่าง กลุ่มควบคุม และความน่าเชื่อถือของวารสาร
4. ให้ระดับหลักฐาน
5. หาภาพประกอบตามลำดับชั้น A ถึง D ก่อนใช้ทางเลือกให้ผู้ใช้ถ่ายรูปเอง
6. เขียนไฟล์เดียวใน `src/lib/manual/species/`
7. ตรวจความปลอดภัย รวมถึงปฏิกิริยาข้ามขั้น
8. รันเทสต์ ดูที่ `/admin/manual/<slug>` แล้วให้เจ้าของระบบอนุมัติ
9. กรอกแบบฟอร์มบันทึกการค้นหลักฐาน

ถ้าพืชนั้นใช้ลำดับขั้นเดิม **ไม่ต้องแตะแกนกลางเลย**

---

## 12 · สิ่งที่ยังเปิดอยู่

- **การอัปโหลดรูปยังไม่เคยทดสอบจริง** ต้องมี Firebase Auth ผู้ใช้จริงกับ Cloudinary credentials
  รายการที่ต้องยืนยันอยู่ใน `handoff.md`
- **ภาพตำแหน่งตัด** ยังไม่มีภาพตรงพันธุ์ในแหล่งที่ใบอนุญาตเปิด ตาม `newplant_protocol.md`
  ส่วนที่ 5 ควรลองชั้น B (แผ่นตัวอย่างพรรณไม้) และชั้น D (พันธุ์ใกล้เคียง) ก่อน แล้วค่อยใช้
  ทางเลือกสุดท้ายคือให้ผู้ใช้ถ่ายรูปต้นตัวเองแล้ววางเครื่องหมายทับ
- **บันทึกต้นแม่เดิมเข้าถึงไม่ได้** ต่างจากล็อตเก่าที่มีหน้าอ่านให้
- **การล็อกเส้นทางอุปกรณ์กับรอบเพาะ** และการบังคับกระปุกเปล่าคุมเมื่อเส้นทางเป็น `unsupported`
- **โหมดมืดของโซนหลังบ้าน** ยังใช้ `globals.css` เดิมที่ทำไว้สำหรับโหมดสว่างอย่างเดียว

---

## 13 · เอกสารอื่น

| อยากรู้ | อ่านที่ |
|---|---|
| ทำไมระบบถึงออกมาเป็นแบบนี้ | `docs/superpowers/redesign-record.md` |
| วิธีทำคู่มือพืชใหม่ | `docs/superpowers/newplant_protocol.md` |
| การตัดสินใจเชิงออกแบบ | `docs/superpowers/specs/2026-08-02-manual-first-redesign-design.md` |
| แต่ละเฟสสร้างยังไง | `docs/superpowers/plans/2026-08-0*.md` |
| ผลตรวจของจริงแต่ละรอบ | `handoff.md` |
| การออกแบบเมนูนำทาง + เครื่องคำนวณลอยทับ | `docs/superpowers/specs/2026-08-03-nav-and-calculators-design.md` |
| **ทำไมถึงเปลี่ยนมาใช้ทรงการเติบโตเป็นแกน** | `docs/superpowers/specs/2026-08-05-growth-form-first-redesign-design.md` |
| แผน implementation ของชั้นทรง (เฟส 0) | `docs/superpowers/plans/2026-08-05-growth-form-foundation.md` |
| แผน implementation ของเฟส 1 (nav + calculator) | `docs/superpowers/plans/2026-08-04-nav-and-calculators.md` |

**หมายเหตุ** ไฟล์ใน `plans/` และ `specs/` ที่ลงวันที่เดือนกรกฎาคม 2026 เป็นของงานก่อนหน้า
ไม่ใช่เอกสารปัจจุบัน

---

## 14 · เมนูนำทาง และเครื่องคำนวณลอยทับ (เพิ่ม 4 สิงหาคม 2026)

เฟส 1 ของงานปรับ UX/UI ที่ผู้ใช้รู้สึกว่าระบบเดิม "แข็งกระด้าง" (ดูเหตุผลเต็มใน
`docs/superpowers/specs/2026-08-03-nav-and-calculators-design.md`) แก้สองเรื่อง:
ก่อนหน้านี้ `GuideShell` มีแค่แถบหัวโชว์ชื่อแบรนด์กับปุ่มสลับธีม **ไม่มีเมนูนำทางเลย**
และเครื่องคำนวณสองในสามตัว (working stock, haiter) **ไม่มี UI เรียกใช้เลย**

**ระบบนำทาง** คอมโพเนนต์ใหม่ `src/components/nav/primary-nav.tsx` เรนเดอร์เมนูเดียวกัน
2 รูปแบบสลับด้วย CSS breakpoint 768px: bottom tab bar คงที่บนมือถือ, แถบแนวนอนในหัวเดิม
บนเดสก์ท็อป มี 3 รายการ หน้าแรก · อุปกรณ์ของฉัน · เครื่องคำนวณ (ปุ่มเปิด overlay ไม่ใช่ลิงก์)

**Calculator overlay** `src/components/calculators/calculator-overlay.tsx` เป็นกล่องลอยทับ
คุมสถานะเปิด/ปิดและจอที่กำลังแสดงด้วย reducer บริสุทธิ์ (`src/components/nav/overlay-state.ts`)
ผ่าน Context (`src/components/nav/calculator-overlay-context.tsx`) มีหน้า picker เลือก 3 ตัว
แล้วสลับไปแสดงฟอร์มโดยไม่เปลี่ยน route สูตรอาหารใช้ `MediumCalculator` เดิมซ้ำ (ไม่แก้ไฟล์นั้น)
ส่วน **Working Stock** (`working-stock-calculator.tsx`) และ **Haiter** (`haiter-calculator.tsx`)
เป็น UI ใหม่ทั้งคู่ ครอบฟังก์ชันคำนวณเดิมที่มีอยู่แล้วแต่ไม่เคยมีใครเรียก ทั้งสองดึงค่าเริ่มต้น
(ความละเอียดเครื่องชั่ง/ปิเปตต์) จาก equipment repository ของผู้ใช้ถ้าล็อกอินอยู่

**โทนภาพ** องค์ประกอบใหม่ทั้งหมดเริ่มใช้ token ชุดนุ่มกว่าเดิม (`--pl-line-soft`,
`--pl-shadow-soft` ใน `src/app/guide.css`) แทนเส้นขอบหนา/เงาทึบแบบ `.pl-card` เดิม
เป็นจุดเริ่มของเฟส 2 ที่จะ retrofit ทั้งระบบทีหลัง (ยังไม่ทำในรอบนี้)

ไฟล์ที่เพิ่ม: `src/components/nav/{overlay-state,calculator-overlay-context,nav-items,primary-nav}.tsx`
และ `src/components/calculators/{calculator-field,working-stock-calculator,haiter-calculator,calculator-overlay}.tsx`
พร้อมเทสต์คู่กันทุกไฟล์ (`renderToStaticMarkup` ตามธรรมเนียมเดิม ไม่มี jsdom)
ไฟล์ที่แก้: `src/app/guide.css`, `src/components/guide/guide-shell.tsx` (mount provider/nav/overlay)

**ที่ยังไม่ทำในรอบนี้** ปรับโทนสี/เงา/เส้นขอบของหน้าที่มีอยู่แล้วทั้งระบบ (เฟส 2), โซนหลังบ้าน
(`LabShell`) ยังไม่มี bottom tab bar หรือ overlay, การลาก handle บาร์เพื่อปิด overlay ทำแบบ
แตะปิดแทนการลากจริง (ตัดสินใจลดความซับซ้อนไว้ในแผน), เมนู "รอบเพาะของฉัน"/"โปรไฟล์" ตั้งใจ
ไม่ใส่ใน nav หลักตามที่ผู้ใช้เลือก, ยังไม่ได้ทำ interactive QA ผ่านเบราว์เซอร์จริง
(เช็กด้วย `renderToStaticMarkup` + smoke test ผ่าน curl เท่านั้น เพราะ `npm run ui:verify`
เดิมอ้างอิงเส้นทาง/คลาสของ UI รุ่นก่อนหน้าที่ไม่ตรงกับโครงสร้างปัจจุบันแล้ว ยังไม่ได้แก้ในรอบนี้)

---

## 15 · ประตูและหน้าทรง (เพิ่ม 5 สิงหาคม 2026)

เฟส 1 ของ redesign รอบสอง เปิดหน้าจอที่ใช้ชั้นทรงจากเฟส 0
เหตุผลเต็มอยู่ใน `docs/superpowers/specs/2026-08-05-growth-form-first-redesign-design.md`

### สี่ประตูแทนการเลือกสายพันธุ์

หน้าแรกเดิมถาม "จะเพาะต้นอะไรดี" แล้วแสดงชื่อวิทยาศาสตร์ ซึ่งมือใหม่ที่ไม่รู้ชื่อต้นของตัวเอง
ตอบไม่ได้ ตอนนี้เป็นสี่ประตูขนาดเท่ากัน ไม่ชี้นำ เพราะยังไม่มีข้อมูลว่าคนส่วนใหญ่มาจากประตูไหน

### ทุก flow ขับด้วย URL ไม่ใช่ state ในเบราว์เซอร์

`/find?stem=vine&node=visible` · `/search?q=` · `/problem?symptom=` ทำให้ทุกหน้ายังเป็น
Server Component ล้วน ทดสอบด้วย `renderToStaticMarkup` ได้ และใช้งานได้แม้ JavaScript ไม่ทำงาน
**ไม่มี client component ใหม่ในเฟสนี้เลย** ช่องค้นหาเป็นฟอร์ม GET ธรรมดา

### คำอธิบายศัพท์ใช้ `<details>` ของ HTML

ไม่ใช่ overlay ที่ต้องใช้ JS ด้วยเหตุผลเดียวกัน `RichText` แปลง `[[id|ข้อความ]]` เป็น
`<details>` ที่แตะกางดูได้ **ข้อจำกัดที่ตามมา** คือ `<details>` ซ้อนใน `<a>` ไม่ได้ตามมาตรฐาน HTML
การ์ดที่ทั้งใบเป็นลิงก์ (เช่นใน `step-map.tsx`) จึงใช้ `plainText` ส่วน `RichText` ใช้ที่
หน้าขั้นเดียวและหน้าทรง ซึ่งไม่ได้อยู่ในลิงก์

**กับดักที่ต้องรู้** เบราว์เซอร์ห่อเนื้อหาใน `<details>` ด้วย pseudo-element `::details-content`
ซึ่งเป็นกล่องบล็อกเต็มความกว้างเสมอ แม้ตอนปิด ทำให้ประโยคขาดกลางคัน ต้องปิดที่ pseudo-element
ไม่ใช่ที่ลูก ดู `.pl-term:not([open])::details-content` ใน `guide.css`

### เส้นทางที่ยังไม่มีของ ต้องบอกตรง ๆ

`forms/finder.ts` มีธง `planned` สำหรับทรงที่อยู่ในแผนแต่ยังไม่ได้เขียน หน้าจอจะบอกว่า
ยังไม่มีคู่มือของทรงนั้น **แทนที่จะซ่อนตัวเลือกจนผู้ใช้คิดว่าต้นของตัวเองไม่มีในระบบ
และแทนที่จะยกคู่มือของทรงอื่นมาให้** เพราะตำแหน่งตัดของแต่ละทรงต่างกันจริง
เทสต์บังคับว่าปลายทางที่อ้างว่ามีอยู่ ต้องมีในทะเบียนทรงจริง

หน้าค้นหาก็เช่นกัน กรณีค้นไม่เจอคือกรณีที่สำคัญที่สุด เพราะระบบมีคู่มือสามชุด
แปลว่าเกือบทุกคนที่เข้ามาจะไม่เจอต้นของตัวเอง

### สองสำเนียงจาก token ชุดเดียว

โหมดอ่านเป็นค่าตั้งต้น เส้นบาง เงานุ่ม หัวข้อ serif เพื่อถอยให้ภาพต้นจริงเป็นประธานของหน้า
โหมดลงมือคือ `.pl-do` ที่ครอบตัวเดินขั้นตอน ต่างกันแค่คอนทราสต์และขนาดตัวเลข
**ไม่ใช่ design system คนละชุด** ภาษาเดิม Illustrated Step Book (เส้น 2.5px เงาทึบ 5px) ถูกแทนที่

### เมนูนำทาง

สี่ช่องคือขีดจำกัดของ bottom tab bar บนจอ 360px "รอบเพาะของฉัน" ถูกสลับออกให้ "แก้ปัญหา"
เพราะคนที่ขวดมีปัญหาต้องเข้าถึงเร็วที่สุด ส่วนรอบเพาะเข้าจากปุ่มท้ายหน้าคู่มือแทน

**`primary-nav.tsx` ค้นไอคอนจาก key ของเมนู** ถ้า key ไม่มีไอคอน React จะเรนเดอร์ `undefined`
แล้วพังทั้งหน้าที่ใช้ `GuideShell` รวมถึงตอน prerender หน้าคู่มือ มีเทสต์บังคับที่ `nav-items.test.ts`

### ของค้างของเฟสนี้

- **ความกว้างมือถือยังไม่ได้ตรวจด้วยตา** รายละเอียดใน `handoff.md`
- **`npm run ui:verify` ยังอ้างอิง UI รุ่นก่อนหน้า** ต้องเขียนใหม่
- **โหมดลงมือ (`.pl-do`) ยังไม่ได้เห็นของจริง** เพราะต้องล็อกอินก่อน

---

## 16 · ภาพอ้างอิงของทรง (เพิ่ม 6 สิงหาคม 2026)

เฟส 3a เปิดชั้นภาพบนหน้าทรง เพื่อตอบคำถามที่ข้อความตอบไม่ได้ คือ "ตรงไหนของต้นคือจุดนั้น"
สเปกอยู่ที่ `docs/superpowers/specs/2026-08-06-form-reference-images-design.md`

### หมุดวางด้วยเปอร์เซ็นต์ ไม่ใช่พิกเซล

`Landmark.point` และ `CutMarker.point` เก็บค่า 0 ถึง 1 เทียบกับขนาดภาพ
ทำให้ภาพย่อขยายตามความกว้างจอได้โดยหมุดไม่หลุดตำแหน่ง และเปลี่ยนภาพเป็นความละเอียดอื่นได้
โดยไม่ต้องแก้ตัวเลขทุกตัว **มีพิกัดได้เมื่อทรงนั้นมี `referenceImageId` เท่านั้น** เทสต์บังคับไว้
เพราะพิกัดที่ไม่มีภาพรองรับคือข้อมูลที่ไม่มีทางแสดงผลได้ และจะเงียบหายไปโดยไม่มีใครรู้

### ภาพขยายเฉพาะจุดคำนวณเป็นพิกเซล

`forms/crop.ts` แปลงพิกัดเปอร์เซ็นต์เป็น `background-position` และ `background-size` เป็นพิกเซล
เพื่อขยายเฉพาะบริเวณรอบหมุดในกรอบเล็กข้างคำอธิบาย เป็นฟังก์ชันบริสุทธิ์ ทดสอบได้โดยไม่ต้องมี DOM

### คำนิยามของจุดสังเกตเป็น `botanical-fact` เสมอ

"ข้อคือปุ่มที่ใบเคยติดอยู่" เป็นคำนิยาม ไม่ใช่ข้ออ้างว่าควรตัดตรงนั้น จึงไม่ถูกนับในกฎจุดอ่อนที่สุด
แต่ยังต้องระบุแหล่งอยู่ดี เพื่อไม่ให้ระดับนี้กลายเป็นทางเลี่ยงการอ้างอิง

### ของค้างของเฟสนี้

- **ยังไม่มีภาพจริงสักภาพ** ทั้งเฟสทำงานบนโครงสร้างล้วน ต้องรอเจ้าของถ่ายรูป
  ลำดับความสำคัญคือสามทรงที่ยากที่สุด คือกอซ้อนกาบ กล้วยไม้ และไม้พุ่มเนื้อแข็ง
- **เฟส 3b ติดที่ Cloudinary credentials** ยังอัปโหลดจริงไม่ได้

---

## 17 · ขั้นทดสอบช่วง (เพิ่ม 6 สิงหาคม 2026)

เฟส 4 ตอบ painpoint ที่เจ้าของยังไม่ได้ตั้งชื่อเอง คือ **วงรอบป้อนกลับสามสัปดาห์**
คนเพาะลองค่าหนึ่งค่า รออีกสามสัปดาห์จึงรู้ผล แล้วได้ข้อมูลกลับมาแค่ "ผ่าน" หรือ "ไม่ผ่าน"
สเปกอยู่ที่ `docs/superpowers/specs/2026-08-06-bracket-test-design.md`

### แบ่งสามชุดในรอบเดียว แทนที่จะลองทีละค่า

`buildBracketPlan()` แตกค่าช่วงเป็นชุด A ต่ำ · B กลาง · C สูง ให้ลองพร้อมกันในรอบเดียว
ได้เส้นโค้งแทนที่จะได้จุดเดียว **และถ้าล้มทั้งสามชุด ก็ยังรู้ว่าควรขยับไปทางไหน**
จากรูปแบบของความล้มเหลว คือติดเชื้อหมดแปลว่าอ่อนไป ตายหมดแปลว่าแรงไป

### คืน `null` เมื่อขั้นนั้นมีหลักฐานตรงชนิดอยู่แล้ว

ไม่มีเหตุผลให้คนเสียกระปุกไปทดสอบสิ่งที่มีงานวิจัยตรงพันธุ์ตอบไว้แล้ว
การทดสอบช่วงมีไว้เติมช่องว่าง ไม่ใช่แทนที่หลักฐาน

### ค่าที่ทดสอบได้เอง ไม่ยกระดับป้ายหลักฐาน

สามกระปุกต่อชุดในรอบเดียวคือจุดตั้งต้นที่ดีกว่าเดา **ไม่ใช่ข้อพิสูจน์** ป้ายจึงยังเป็น
"ยังไม่มีงานรองรับ" เหมือนเดิม และหน้ารายการเขียนข้อจำกัดนี้ไว้ตรง ๆ ใต้ตัวเลขทุกครั้ง
ถ้ายอมให้ยกระดับ ป้ายหลักฐานจะเลื่อนความหมายจาก "มีใครตรวจสอบได้" เป็น "ฉันลองแล้วได้ผล"
ซึ่งทำลายเหตุผลที่ระบบนี้มีป้ายตั้งแต่แรก

### ค่าช่วงรวมทีละคีย์ ไม่ทับกันทั้งก้อน

ต่างจากฟิลด์อื่นใน `resolve.ts` เพราะชั้นทรงอาจให้ค่าหลายคีย์แล้วชั้นสกุลทับเพียงคีย์เดียว
ถ้าทับทั้งก้อน คีย์ที่เหลือจะหายเงียบ ๆ และต้องรวมทั้งค่าระดับทรง/สกุล
กับค่าที่ให้มากับขั้นนั้นโดยตรง เพราะทั้งสองทางเป็นที่ที่คนเขียนใส่ค่าได้จริง

### ของค้างของเฟสนี้

- **โหมดล็อกอินจริงกับ Firestore ยังไม่ได้ตรวจ** ตรวจในโหมดสาธิตเท่านั้น
- **กรณีล้มทั้งสามชุดยังไม่มีหน้าจอบอก** ตรรกะตัดสินทิศทางถูกต้องและมีเทสต์ครบ
  แต่ตอนนี้ระบบแค่ไม่บันทึกค่า ซึ่งเงียบเกินไปสำหรับคนที่เพิ่งเสียเก้ากระปุก
- **ตารางห้าคอลัมน์บนจอ 360px ยังไม่ได้เห็นของจริง**
## 16 · ชั้นภาพของทรง (เพิ่ม 6 สิงหาคม 2026)

เฟส 3a ของ redesign รอบสอง เหตุผลเต็มอยู่ใน
`docs/superpowers/specs/2026-08-06-form-reference-images-design.md`

**หนึ่งทรง หนึ่งภาพ** ภาพเดียวมีหมุดของทุกจุดสังเกต เพราะมือใหม่ต้องเห็นก่อนว่าจุดต่าง ๆ
อยู่ตรงไหนเทียบกัน ซึ่งเป็นสิ่งเดียวที่ภาพให้ได้แต่ข้อความให้ไม่ได้

**ภาพของทรงคือภาพต้นจริงของชนิดหนึ่งที่เป็นตัวแทน** จึงต้องติดคำบรรยายบอกชนิดในภาพเสมอ
ตามกฎชั้น D ของ `newplant_protocol.md` ขั้นที่ 5 และต้องพกเครดิตกับใบอนุญาต CC BY-SA 4.0
ติดตัวมาด้วยเพราะไฟล์ commit ขึ้น public repo

**หมุดวางด้วยเปอร์เซ็นต์ ภาพซูมคำนวณเป็นพิกเซล** หมุดใช้เปอร์เซ็นต์จึงสเกลตามภาพเองโดยไม่ต้องใช้
JavaScript ส่วนช่องซูมของแต่ละการ์ดใช้ `cropStyle` คำนวณเป็นพิกเซล เพราะ `background-position`
แบบเปอร์เซ็นต์จัดให้จุด p ของภาพไปตรงกับจุด p ของกรอบ ซึ่งไม่ใช่การวางจุดนั้นไว้กลางกรอบ
และจะเพี้ยนหนักที่สุดกับจุดที่อยู่ริมภาพ

**`/admin/pin` เป็นข้อยกเว้นของกฎห้ามเพิ่ม client component** เพราะการรับพิกัดจากการคลิก
ต้องใช้ JavaScript จริง ยกเว้นเฉพาะโซน `/admin` ซึ่งไม่ใช่หน้าผู้ใช้
หน้าสาธารณะทุกหน้ายังเป็น Server Component ล้วนตามเดิม

**ยังไม่มีภาพจริงสักใบ** ทุกทรงยังแสดงกล่องว่ายังไม่มีภาพอ้างอิง กลไกพร้อมแล้วและมีเทสต์คุมครบ
รอเจ้าของระบบถ่ายภาพแล้วทยอยเติมทีละทรง วิธีเติมคือวางไฟล์ที่ `public/forms/<formId>.jpg`
เพิ่ม `referenceImage` ในไฟล์ทรง แล้วเปิด `/admin/pin` คลิกปักหมุดและคัดลอกพิกัดกลับไปวาง

**สิ่งที่ยังไม่ได้ตรวจ** หมุดสีเขียวยังไม่เคยเห็นบนภาพต้นไม้จริงที่เขียวทั้งภาพ
อาจต้องเปลี่ยนสีหรือเพิ่มเงาเมื่อมีภาพจริงใบแรก บันทึกไว้ใน `handoff.md`
