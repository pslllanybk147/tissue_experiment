# Plantlover Lab — สรุปโครงสร้างระบบทั้งหมด

> อัปเดต 4 สิงหาคม 2026 · เทสต์ 322 ผ่าน 10 skip · lint สะอาด · build ผ่าน
>
> เอกสารนี้อธิบาย **โครงสร้างของระบบตั้งแต่ต้นจนจบ** ว่ามีอะไรบ้าง แต่ละส่วนทำหน้าที่อะไร
> และเชื่อมกันอย่างไร ถ้าอยากรู้ว่า **ทำไม** ถึงออกมาเป็นแบบนี้ อ่าน `docs/superpowers/redesign-record.md`

---

## 1 · ระบบนี้คืออะไร

เว็บคู่มือเพาะเลี้ยงเนื้อเยื่อพืชแบบทีละขั้น ที่ **บอกระดับความน่าเชื่อถือของทุกคำแนะนำ**
และคำนวณปริมาณสารให้ทำได้จริงด้วยอุปกรณ์ที่ผู้ใช้มี

หลักการที่คุมทั้งระบบ

1. **อ่านก่อนได้ ลงมือทีหลัง** คู่มืออ่านครบได้โดยไม่ต้องล็อกอิน
2. **คู่มือบอกว่าต้องได้อะไร ระบบบอกว่าด้วยของที่มีจะได้มายังไง**
3. **ทุกคำแนะนำต้องบอกได้ว่ามาจากไหน** และถ้าไม่มีที่มา ต้องบอกว่าค้นอะไรไปแล้ว
4. **ห้ามแสดงตัวเลขที่ทำตามไม่ได้** ถ้าชั่งไม่ได้ต้องบอกตรง ๆ แล้วเสนอทางแก้

---

## 2 · แผนผังเส้นทาง สามโซน

### โซนสาธารณะ · ไม่ต้องล็อกอิน

| เส้นทาง | หน้าที่ |
|---|---|
| `/` | เลือกต้นที่จะเพาะ ถามคำถามเดียว |
| `/guide/<slug>` | แผนที่ 14 ขั้นของพืชนั้น พร้อมระดับหลักฐานรายขั้น |
| `/guide/<slug>/step/<n>` | เนื้อหาขั้นเดียว ภาพประกอบ เกณฑ์ผ่าน จุดที่ต้องหยุด อาการที่อาจเจอ |

หน้าเหล่านี้เป็น Server Component ล้วน prerender ตอน build อ่านจากไฟล์ในโค้ด ไม่แตะฐานข้อมูล

### โซนผู้ใช้ · ต้องล็อกอิน

| เส้นทาง | หน้าที่ |
|---|---|
| `/my` | หน้าสรุปหลังบ้าน |
| `/my/rounds` | รายการรอบเพาะ พร้อมความคืบหน้า และรอบเก่าที่แยกไว้ |
| `/my/rounds/new?slug=<slug>` | สร้างรอบจากคู่มือแล้วพาไปหน้ารอบ |
| `/my/rounds/<id>` | ภาพรวมทั้ง 14 ขั้น ไฮไลต์ว่าทำต่อตรงไหน |
| `/my/rounds/<id>/step/<n>` | ตัวเดินขั้นตอน บันทึกค่า จดข้อความ แนบรูป |
| `/my/rounds/legacy/<id>` | อ่านรอบที่สร้างก่อนระบบคู่มือใหม่ แบบอ่านอย่างเดียว |
| `/my/equipment` | บอกของที่มี แล้วระบบจัดเส้นทางให้ |

### โซนหลังบ้าน

| เส้นทาง | หน้าที่ |
|---|---|
| `/admin/manual` และ `/admin/manual/<slug>` | กางคู่มือฉบับประกอบเสร็จเพื่อตรวจทาน |
| `/admin/knowledge` | ทะเบียน taxon แหล่งอ้างอิง และ claim ที่รออนุมัติ |
| `/admin/research` | ตรวจงานวิจัย |
| `/admin/dataset-review` | ตรวจรูปสำหรับงาน ML |

---

## 3 · โครงเนื้อหาคู่มือ · `src/lib/manual/`

**แกนกลางเป็นคลังขั้นตอน ไม่ใช่ลำดับตายตัว**

```
types.ts          สคีมา ManualStepDef, PlantPack, EvidenceRef, MediaRecipe, TroubleshootingEntry
core-steps.ts     คลังขั้นตอนกลาง 14 ขั้น ใช้ร่วมกันทุกพืช
troubleshooting.ts คลังอาการกลาง เช่น การดำจากฟีนอลิก กับการดำจากฟอกแรงเกิน
sources.ts        ทะเบียนแหล่งอ้างอิงพร้อมใบอนุญาต
resolve.ts        ประกอบแกนกลาง + แผ่นเสริม เป็นคู่มือฉบับสมบูรณ์
registry.ts       รวมแผ่นเสริมทั้งหมด และ resolve ตาม slug
summary.ts        นับสถิติของคู่มือ ใช้ในหน้าตรวจทาน
species/          แผ่นเสริมรายชนิด หนึ่งไฟล์ต่อหนึ่งพืช
```

แผ่นเสริมมีสี่ส่วน

- `sequence` เลือกและเรียงขั้นเอง **ถอดขั้นที่ไม่เกี่ยวออกได้**
- `values` และ `overrides` ทับค่าหรือข้อความของขั้นจากแกนกลาง
- `steps` **เขียนขั้นใหม่ที่แกนกลางไม่มีได้เต็มขั้น**
- `mediaRecipes` สูตรอาหารของพืชนั้น

ผลคือพืชที่วิธีต่างกันสิ้นเชิง เช่น Anthurium ที่ขยายผ่าน callus เขียนได้ในไฟล์เดียว
โดยไม่ต้องแตะแกนกลาง และขั้นที่เหมือนกันจริง ๆ ยังใช้ต้นฉบับร่วมกัน แก้ที่เดียวได้ทุกพืช

**พืชที่มีคู่มือแล้ว** Pink Princess, Violin variegated, และเส้นทางกลางของ Philodendron ที่ยังไม่ยืนยันชนิด

---

## 4 · ระบบหลักฐาน

สามระดับ `species-direct` ตรงพันธุ์ · `adapted` ประยุกต์ · `unsupported` ยังไม่มีงานรองรับ

**กฎที่บังคับด้วยเทสต์ ห้ามแก้เทสต์เพื่อให้ผ่าน**

| กฎ | ไฟล์ที่บังคับ |
|---|---|
| ข้ออ้างที่ว่ามีงานรองรับ ต้องระบุ `sourceIds` | `evidence-rules.test.ts` |
| ข้ออ้างที่ว่าไม่มีงานรองรับ ต้องบันทึก `searchQueries` และ `searchedAt` | `evidence-rules.test.ts` |
| ทุก `sourceId` ที่อ้าง ต้องมีอยู่จริงในทะเบียน | `sources.test.ts` |
| ทุก `troubleshootingId` ที่อ้าง ต้องมีอยู่จริง | `core-steps.test.ts` |
| ทุก `illustrationId` ที่อ้าง ต้องมีภาพจริง | `illustrations.test.tsx` |

กฎข้อสองคือหัวใจ **การบอกว่า "ไม่มีงานวิจัยรองรับ" ต้องมีหลักฐานของตัวเองเหมือนกัน**
เพื่อให้ตรวจย้อนได้ว่าช่องว่างนั้นจริง หรือแค่ค้นไม่พอ และรู้ว่าควรกลับไปค้นใหม่เมื่อไหร่

**กฎจุดอ่อนที่สุด** ระดับของเส้นทางรวมเท่ากับจุดที่อ่อนที่สุด ไม่ใช่จุดที่แข็งที่สุด

**กฎเนื้อหาสำหรับมือใหม่ (ยังไม่มีเทสต์บังคับ ตรวจด้วยคนตอนรีวิว เพิ่ม 4 สิงหาคม 2026
หลังพบว่าขั้นตัด/ฟอกของ Violin ด่างมีจุดที่มือใหม่ตามทำไม่ได้จริง)**

1. **ทุกครั้งที่พูดถึงสารเคมีหรือสารใด ๆ ในเนื้อหา ห้ามเอ่ยแค่ชื่อสารลอย ๆ** ต้องระบุครบ
   - ชื่อสารและชนิด/รูปแบบที่ใช้จริง (เช่น ผง สารละลายสำเร็จรูป น้ำยาฟอกขาวเชิงพาณิชย์)
   - ปริมาณที่ใช้ และถ้าผสมน้ำ ต้องบอก**อัตราส่วนต่อปริมาตร** (เช่น ต่อลิตร) กับ**ความเข้มข้นที่ได้จริง**
     ไม่ใช่แค่ "1%" ลอย ๆ (ดูตัวอย่างความกำกวมของหน่วยในส่วนที่ 2 ข้อสาม)
   - ระยะเวลาที่ใช้หรือแช่
   - หาซื้อ/หาได้จากไหน โดยเฉพาะของที่หาซื้อตามบ้านไม่ได้ ต้องบอกทางเลือกที่ใช้แทนได้
   ตัวอย่างที่แก้แล้ว: "สารต้านออกซิเดชัน" เดิมไม่ระบุชื่อ แก้เป็น "สารละลายกรดแอสคอร์บิก (วิตามินซี)
   หรือกรดซิตริก" ใน `troubleshooting.ts`
2. **ทุกครั้งที่ทำภาพประกอบที่เกี่ยวกับการตัด ต้องระบุตำแหน่งตัดให้แม่นยำ** ทั้งในภาพ (จุดตัด/ข้อ
   ต้องอยู่บนโครงสร้างของพืชจริง ไม่ใช่อยู่บนองค์ประกอบตกแต่งอื่นในภาพ) และในข้อความกำกับ (ระยะเป็น ซม.
   หรือ มม. นับจากจุดอ้างอิงที่ชัดเจน เช่น "จากปลายยอด" ไม่ใช่แค่มีสัญลักษณ์กรรไกรอยู่ใกล้ ๆ) —
   ดูเคสตัวอย่างที่แก้แล้วใน `illustrations.tsx` (`cut-explant-violin-bipennifolium`) และ
   actions ของขั้น `cut` ใน `violin-variegated.ts`

---

## 5 · รอบเพาะ · `src/lib/rounds/`

**ไม่มี collection ใหม่** รอบเพาะแมปลงบนโครงข้อมูลเดิม จึงไม่ต้อง migrate และไม่ต้องแก้ security rules

| แนวคิดใหม่ | เก็บเป็น |
|---|---|
| รอบเพาะ | `ExperimentLot` โดย `protocolId` เก็บ slug ของคู่มือ |
| บันทึกรายขั้น | `ProtocolStepRun` โดย `stepId` เก็บ id ของขั้นจากแกนกลาง |
| รูปหลักฐานของขั้น | `Observation` ชนิด `protocol-step-evidence` หนึ่งรายการต่อขั้น ทำหน้าที่แขวนรูป |

```
round-adapter.ts   ประกอบมุมมองรอบจาก lot + step runs + คู่มือ
legacy-rounds.ts   แยกรอบเก่าที่ resolve ไม่ได้ออกมา ไม่ให้หายไปเงียบ ๆ
step-evidence.ts   สร้างและค้น observation ที่ใช้แขวนรูป
medium-plan.ts     คำนวณสูตรอาหารตามจำนวนกระปุกและความละเอียดอุปกรณ์
```

**บันทึกที่อ้างขั้นซึ่งคู่มือไม่มีแล้วจะถูกข้าม ไม่ใช่โยน error** เพื่อให้แก้ลำดับขั้นของคู่มือ
ภายหลังได้โดยไม่ทำให้รอบเก่าเปิดไม่ได้

---

## 6 · เส้นทางอุปกรณ์ · `src/lib/equipment/`

กระบวนการต้องการสี่อย่าง **อาหารปลอดเชื้อ · น้ำปลอดเชื้อ · ภาชนะปลอดเชื้อ · ผิวชิ้นพืชสะอาด**

`capabilities.ts` เก็บวิธีที่ให้แต่ละอย่าง พร้อมอุปกรณ์ที่ต้องใช้และระดับหลักฐานของวิธีนั้น
`resolve-path.ts` จับคู่กับของที่ผู้ใช้มี แล้วเลือกวิธีที่หลักฐานดีที่สุดที่ทำได้

**ถามเป็นของที่หาซื้อได้จริง** มีตัวเลือกหม้ออัดแรงดันทำอาหารแยกจากหม้อนึ่งของแล็บ
เพราะที่ 15 psi ได้ 121 องศาเท่ากัน ถ้าถามด้วยคำว่า autoclave คนทำที่บ้านจะติ๊กว่าไม่มี
ทั้งที่มีของใช้แทนได้อยู่ในครัว

**เมื่อทำอย่างใดไม่ได้เลย ระบบกางทางเลือกพร้อมความเสี่ยงและไม่เลือกให้อัตโนมัติ**

ชุดอุปกรณ์เก็บที่ `users/{uid}/settings/equipment` ซึ่ง security rules เดิมอนุญาตอยู่แล้ว

---

## 7 · เครื่องคำนวณสูตรอาหาร

ปัญหาของงานเล็กไม่ใช่การคูณเลข แต่คือ **ปริมาณเล็กจนชั่งไม่ได้**

ลำดับการคิด

1. จำนวนกระปุก บวกกระปุกเปล่าคุมและสำรอง คูณปริมาตรต่อกระปุก บวกเผื่อสูญเสีย
2. คูณสูตรตามปริมาตรที่ได้
3. เทียบทุกบรรทัดกับความละเอียดของเครื่องชั่งที่ผู้ใช้ระบุ
4. บรรทัดที่ชั่งไม่ได้ **ห้ามปัดเศษ** ให้เรียก `calculateWorkingStock` เขียนวิธีทำน้ำยาแม่แทน
   โดย **เริ่มจากการชั่งผง** ที่มวลสูงกว่าขีดต่ำสุดของเครื่องชั่งห้าเท่า ไม่ใช่พอดีเป๊ะ

โมดูลคำนวณที่ยังใช้อยู่ `medium-batch-calculations`, `working-stock-calculator`, `haiter-calculations`

ทั้งสามตัวมี UI เรียกใช้ได้จริงแล้วผ่านเมนู "เครื่องคำนวณ" (ดูส่วนที่ 14) ก่อนหน้านี้มีแค่สูตรอาหาร
ที่ฝังอยู่ในขั้น `prep-media` เท่านั้น อีกสองตัวเป็นฟังก์ชันบริสุทธิ์ที่ทดสอบไว้แต่ไม่มีใครเรียกใช้

---

## 8 · ชั้นข้อมูล

ทุกอย่างอ่านเขียนผ่าน **repository interface** ห้ามเรียก Firestore SDK จาก component

แต่ละ repository มีสามส่วน คือ interface, ตัวจำในหน่วยความจำสำหรับโหมดสาธิต
ที่เขียนผ่าน `demo-storage` ลง localStorage, และตัว Firestore สำหรับผู้ใช้จริง
โดย factory เลือกให้ตามสถานะการล็อกอิน

repository ที่มี `experiment`, `step-run`, `media`, `equipment`, `dataset`,
`knowledge-library`, `knowledge-source`, `lab`

**ออฟไลน์** Firestore เปิด `persistentLocalCache` ไว้ จดข้อความและตัวเลขได้ตอนเน็ตหลุด
แล้วซิงก์เองเมื่อกลับมา **แนบรูปไม่ได้** และระบบอธิบายเหตุผลแทนการซ่อนปุ่มเงียบ ๆ

ข้อมูลทั้งหมดอยู่ใต้ `users/{uid}` และ security rules บังคับว่า `ownerId` ต้องตรงกับเจ้าของ

---

## 9 · ชั้นดีไซน์

ภาษา **Illustrated Step Book** เส้นขอบหนา เงาทึบไม่เบลอ สีสด ภาพประกอบทุกขั้น

`src/app/guide.css` เก็บ token ขึ้นต้น `--pl-` และ class ขึ้นต้น `pl-`
**แยกจาก `globals.css` เดิมทั้งหมด** เพื่อไม่ให้ชนกัน

**โหมดมืดออกแบบที่ระดับ token ตั้งแต่ต้น ไม่ใช่ invert ทีหลัง** ตอบสนองทั้ง
`prefers-color-scheme` และปุ่มสลับ โดยปุ่มชนะ media query ทั้งสองทาง
ธีมถูกตั้งด้วย inline script ก่อน hydrate เพื่อไม่ให้หน้ากระพริบ

ภาพประกอบ 15 ภาพใน `illustrations.tsx` เป็น SVG ที่ **ใช้สีจาก token ทั้งหมด**
จึงเปลี่ยนตามโหมดได้จริง

---

## 10 · แนวทางเทสต์

- เทสต์ component ใช้ `renderToStaticMarkup` ไม่มี jsdom ไม่มี testing-library
- component ที่แสดงผลเป็น sync ทั้งหมด ส่วน async อยู่ที่ `page.tsx` เท่านั้น
  เพื่อให้เทสต์ได้โดยไม่ต้อง mock
- ตรรกะคำนวณเป็นฟังก์ชันบริสุทธิ์ แยกจาก React
- `npm run ui:verify` ตรวจ 14 ความกว้างตั้งแต่ 360 ถึง 1920 px บน production build

**ข้อจำกัดที่ต้องรู้** วิธีนี้จับพฤติกรรมข้ามหน้าและข้ามการโหลดใหม่ไม่ได้
ข้อบกพร่องหลายข้อในโปรเจกต์นี้จึงเจอจากการเปิดดูของจริงเท่านั้น รายละเอียดอยู่ในส่วนที่ 6
ของ `docs/superpowers/newplant_protocol.md`

---

## 11 · วิธีเพิ่มพืชชนิดใหม่

ทำตามเก้าขั้นใน `docs/superpowers/newplant_protocol.md` (มาแทน `manual-authoring-protocol.md`
เดิม ซึ่งตอนนี้เหลือแค่ตัวชี้มาที่ไฟล์นี้) โดยย่อคือ

1. ยืนยันชื่อวิทยาศาสตร์จาก POWO หรือ WCVP
2. ค้นหลักฐานให้ครบสิบเอ็ดช่องทาง **รวมชื่อพ้อง ชื่อพันธุ์พี่น้อง วิทยานิพนธ์ สิทธิบัตร
   และฟอรัมนักปลูก (ระดับ anecdotal เท่านั้น)**
3. คัดกรองแปดข้อ โดยข้อสำคัญที่สุดยังคงเป็น **ตรวจว่างานนั้นเริ่มจากต้นแม่จริงหรือจากเนื้อเยื่อ
   ที่ปลอดเชื้ออยู่แล้ว** เพิ่มเติมคือตรวจขนาดตัวอย่าง กลุ่มควบคุม และความน่าเชื่อถือของวารสาร
4. ให้ระดับหลักฐาน
5. หาภาพประกอบตามลำดับชั้น A ถึง D ก่อนใช้ทางเลือกให้ผู้ใช้ถ่ายรูปเอง
6. เขียนไฟล์เดียวใน `src/lib/manual/species/`
7. ตรวจความปลอดภัย รวมถึงปฏิกิริยาข้ามขั้น
8. รันเทสต์ ดูที่ `/admin/manual/<slug>` แล้วให้เจ้าของระบบอนุมัติ
9. กรอกแบบฟอร์มบันทึกการค้นหลักฐาน

ถ้าพืชนั้นใช้ลำดับขั้นเดิม **ไม่ต้องแตะแกนกลางเลย**

---

## 12 · สิ่งที่ยังเปิดอยู่

- **การอัปโหลดรูปยังไม่เคยทดสอบจริง** ต้องมี Firebase Auth ผู้ใช้จริงกับ Cloudinary credentials
  รายการที่ต้องยืนยันอยู่ใน `handoff.md`
- **ภาพตำแหน่งตัด** ยังไม่มีภาพตรงพันธุ์ในแหล่งที่ใบอนุญาตเปิด ตาม `newplant_protocol.md`
  ส่วนที่ 5 ควรลองชั้น B (แผ่นตัวอย่างพรรณไม้) และชั้น D (พันธุ์ใกล้เคียง) ก่อน แล้วค่อยใช้
  ทางเลือกสุดท้ายคือให้ผู้ใช้ถ่ายรูปต้นตัวเองแล้ววางเครื่องหมายทับ
- **บันทึกต้นแม่เดิมเข้าถึงไม่ได้** ต่างจากล็อตเก่าที่มีหน้าอ่านให้
- **การล็อกเส้นทางอุปกรณ์กับรอบเพาะ** และการบังคับกระปุกเปล่าคุมเมื่อเส้นทางเป็น `unsupported`
- **โหมดมืดของโซนหลังบ้าน** ยังใช้ `globals.css` เดิมที่ทำไว้สำหรับโหมดสว่างอย่างเดียว

---

## 13 · เอกสารอื่น

| อยากรู้ | อ่านที่ |
|---|---|
| ทำไมระบบถึงออกมาเป็นแบบนี้ | `docs/superpowers/redesign-record.md` |
| วิธีทำคู่มือพืชใหม่ | `docs/superpowers/newplant_protocol.md` |
| การตัดสินใจเชิงออกแบบ | `docs/superpowers/specs/2026-08-02-manual-first-redesign-design.md` |
| แต่ละเฟสสร้างยังไง | `docs/superpowers/plans/2026-08-0*.md` |
| ผลตรวจของจริงแต่ละรอบ | `handoff.md` |
| การออกแบบเมนูนำทาง + เครื่องคำนวณลอยทับ | `docs/superpowers/specs/2026-08-03-nav-and-calculators-design.md` |
| แผน implementation ของเฟส 1 (nav + calculator) | `docs/superpowers/plans/2026-08-04-nav-and-calculators.md` |

**หมายเหตุ** ไฟล์ใน `plans/` และ `specs/` ที่ลงวันที่เดือนกรกฎาคม 2026 เป็นของงานก่อนหน้า
ไม่ใช่เอกสารปัจจุบัน

---

## 14 · เมนูนำทาง และเครื่องคำนวณลอยทับ (เพิ่ม 4 สิงหาคม 2026)

เฟส 1 ของงานปรับ UX/UI ที่ผู้ใช้รู้สึกว่าระบบเดิม "แข็งกระด้าง" (ดูเหตุผลเต็มใน
`docs/superpowers/specs/2026-08-03-nav-and-calculators-design.md`) แก้สองเรื่อง:
ก่อนหน้านี้ `GuideShell` มีแค่แถบหัวโชว์ชื่อแบรนด์กับปุ่มสลับธีม **ไม่มีเมนูนำทางเลย**
และเครื่องคำนวณสองในสามตัว (working stock, haiter) **ไม่มี UI เรียกใช้เลย**

**ระบบนำทาง** คอมโพเนนต์ใหม่ `src/components/nav/primary-nav.tsx` เรนเดอร์เมนูเดียวกัน
2 รูปแบบสลับด้วย CSS breakpoint 768px: bottom tab bar คงที่บนมือถือ, แถบแนวนอนในหัวเดิม
บนเดสก์ท็อป มี 3 รายการ หน้าแรก · อุปกรณ์ของฉัน · เครื่องคำนวณ (ปุ่มเปิด overlay ไม่ใช่ลิงก์)

**Calculator overlay** `src/components/calculators/calculator-overlay.tsx` เป็นกล่องลอยทับ
คุมสถานะเปิด/ปิดและจอที่กำลังแสดงด้วย reducer บริสุทธิ์ (`src/components/nav/overlay-state.ts`)
ผ่าน Context (`src/components/nav/calculator-overlay-context.tsx`) มีหน้า picker เลือก 3 ตัว
แล้วสลับไปแสดงฟอร์มโดยไม่เปลี่ยน route สูตรอาหารใช้ `MediumCalculator` เดิมซ้ำ (ไม่แก้ไฟล์นั้น)
ส่วน **Working Stock** (`working-stock-calculator.tsx`) และ **Haiter** (`haiter-calculator.tsx`)
เป็น UI ใหม่ทั้งคู่ ครอบฟังก์ชันคำนวณเดิมที่มีอยู่แล้วแต่ไม่เคยมีใครเรียก ทั้งสองดึงค่าเริ่มต้น
(ความละเอียดเครื่องชั่ง/ปิเปตต์) จาก equipment repository ของผู้ใช้ถ้าล็อกอินอยู่

**โทนภาพ** องค์ประกอบใหม่ทั้งหมดเริ่มใช้ token ชุดนุ่มกว่าเดิม (`--pl-line-soft`,
`--pl-shadow-soft` ใน `src/app/guide.css`) แทนเส้นขอบหนา/เงาทึบแบบ `.pl-card` เดิม
เป็นจุดเริ่มของเฟส 2 ที่จะ retrofit ทั้งระบบทีหลัง (ยังไม่ทำในรอบนี้)

ไฟล์ที่เพิ่ม: `src/components/nav/{overlay-state,calculator-overlay-context,nav-items,primary-nav}.tsx`
และ `src/components/calculators/{calculator-field,working-stock-calculator,haiter-calculator,calculator-overlay}.tsx`
พร้อมเทสต์คู่กันทุกไฟล์ (`renderToStaticMarkup` ตามธรรมเนียมเดิม ไม่มี jsdom)
ไฟล์ที่แก้: `src/app/guide.css`, `src/components/guide/guide-shell.tsx` (mount provider/nav/overlay)

**ที่ยังไม่ทำในรอบนี้** ปรับโทนสี/เงา/เส้นขอบของหน้าที่มีอยู่แล้วทั้งระบบ (เฟส 2), โซนหลังบ้าน
(`LabShell`) ยังไม่มี bottom tab bar หรือ overlay, การลาก handle บาร์เพื่อปิด overlay ทำแบบ
แตะปิดแทนการลากจริง (ตัดสินใจลดความซับซ้อนไว้ในแผน), เมนู "รอบเพาะของฉัน"/"โปรไฟล์" ตั้งใจ
ไม่ใส่ใน nav หลักตามที่ผู้ใช้เลือก, ยังไม่ได้ทำ interactive QA ผ่านเบราว์เซอร์จริง
(เช็กด้วย `renderToStaticMarkup` + smoke test ผ่าน curl เท่านั้น เพราะ `npm run ui:verify`
เดิมอ้างอิงเส้นทาง/คลาสของ UI รุ่นก่อนหน้าที่ไม่ตรงกับโครงสร้างปัจจุบันแล้ว ยังไม่ได้แก้ในรอบนี้)
