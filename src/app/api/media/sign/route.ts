import { NextResponse } from "next/server"; import { getCloudinaryConfig } from "../../../../lib/cloudinary/config"; import { buildSignedUpload } from "../../../../lib/cloudinary/signature"; import { getAdminAuth } from "../../../../lib/firebase/admin";
const allowed=new Set(["image/jpeg","image/png","image/webp"]); export async function POST(request:Request){const header=request.headers.get("authorization");if(!header?.startsWith("Bearer "))return NextResponse.json({error:"Authentication required"},{status:401});let uid:string;try{uid=(await getAdminAuth().verifyIdToken(header.slice(7))).uid}catch{return NextResponse.json({error:"Invalid authentication"},{status:401})}let body:{lotId?:string;observationId?:string;mimeType?:string;bytes?:number};try{body=await request.json()}catch{return NextResponse.json({error:"Invalid request"},{status:400})}if(!body.lotId||!body.observationId||!body.mimeType||!allowed.has(body.mimeType)||!Number.isFinite(body.bytes)||body.bytes!<0||body.bytes!>10_000_000)return NextResponse.json({error:"Invalid image"},{status:400});try{const config=getCloudinaryConfig();const signed=buildSignedUpload({uid,lotId:body.lotId,observationId:body.observationId,timestamp:Math.floor(Date.now()/1000),apiSecret:config.apiSecret});return NextResponse.json({...signed,cloudName:config.cloudName,apiKey:config.apiKey})}catch{return NextResponse.json({error:"Media service unavailable"},{status:503})}}
