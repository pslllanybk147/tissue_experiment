import type { ProtocolStepProgress } from "../domain/models";
export type ProgressAuditEvent = { id: string; ownerId: string; lotId: string; stepId: string; action: "completed" | "skipped" | "reopened" | "note_updated"; occurredAt: string };
export interface ProtocolProgressRepository { list(ownerId:string,lotId:string):Promise<ProtocolStepProgress[]>; complete(ownerId:string,lotId:string,protocolId:string,versionId:string,stepId:string,note:string):Promise<ProtocolStepProgress>; skip(ownerId:string,lotId:string,protocolId:string,versionId:string,stepId:string,note:string):Promise<ProtocolStepProgress>; reopen(ownerId:string,lotId:string,stepId:string):Promise<ProtocolStepProgress>; saveNote(ownerId:string,lotId:string,stepId:string,note:string):Promise<ProtocolStepProgress>; listAuditEvents(ownerId:string,lotId:string):Promise<ProgressAuditEvent[]>; }
