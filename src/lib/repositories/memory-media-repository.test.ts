import {describe,expect,it}from"vitest";import{createMemoryMediaRepository}from"./memory-media-repository";
const media={id:"m1",ownerId:"o1",lotId:"l1",observationId:"x1",cloudinaryPublicId:"p",secureUrl:"https://x",width:10,height:10,format:"jpg"as const,bytes:10,caption:"",capturedAt:null,createdBy:"o1",createdAt:"t",updatedAt:"t",deletedAt:null};describe("media repository",()=>{it("saves, deletes and restores idempotently",async()=>{const repo=createMemoryMediaRepository("o1");await repo.save("o1",media);await repo.softDelete("o1","l1","x1","m1");await repo.softDelete("o1","l1","x1","m1");expect((await repo.list("o1","l1","x1",true))[0].deletedAt).not.toBeNull();expect((await repo.listAuditEvents("o1","l1")).filter(e=>e.action==="deleted")).toHaveLength(1);await repo.restore("o1","l1","x1","m1");expect((await repo.list("o1","l1","x1"))[0].deletedAt).toBeNull()})});
