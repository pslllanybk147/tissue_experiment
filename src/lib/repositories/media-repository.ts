import type{ObservationMedia}from"../domain/models";export type MediaAudit={id:string;ownerId:string;lotId:string;mediaId:string;action:"created"|"updated"|"deleted"|"restored";occurredAt:string};export interface MediaRepository{list(ownerId:string,lotId:string,observationId:string,includeDeleted?:boolean):Promise<ObservationMedia[]>;save(ownerId:string,media:ObservationMedia):Promise<ObservationMedia>;updateCaption(ownerId:string,lotId:string,observationId:string,mediaId:string,caption:string):Promise<ObservationMedia>;softDelete(ownerId:string,lotId:string,observationId:string,mediaId:string):Promise<ObservationMedia>;restore(ownerId:string,lotId:string,observationId:string,mediaId:string):Promise<ObservationMedia>;listAuditEvents(ownerId:string,lotId:string):Promise<MediaAudit[]>}
