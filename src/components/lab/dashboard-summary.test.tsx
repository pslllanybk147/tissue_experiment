import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it}from"vitest";import{DashboardSummary}from"./dashboard-summary";import{createDemoSnapshot}from"../../lib/repositories/demo-lab-repository";describe("DashboardSummary",()=>{it("uses real workflow links",()=>{const html=renderToStaticMarkup(<DashboardSummary snapshot={createDemoSnapshot("o1")}/>);expect(html).toContain('href="/admin/manual"');expect(html).toContain('href="/my/rounds"');expect(html).toContain('href="/admin/research"');expect(html).not.toContain("local preview")});it("offers one obvious beginner starting action",()=>{const html=renderToStaticMarkup(<DashboardSummary snapshot={createDemoSnapshot("o1")}/>);expect(html).toContain('href="/my/rounds"');expect(html).toContain("เปิดคู่มือฉบับผู้ใช้")})});
