import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it}from"vitest";import{DashboardSummary}from"./dashboard-summary";import{createDemoSnapshot}from"../../lib/repositories/demo-lab-repository";describe("DashboardSummary",()=>{it("uses real workflow links",()=>{const html=renderToStaticMarkup(<DashboardSummary snapshot={createDemoSnapshot("o1")}/>);expect(html).toContain('href="/protocols"');expect(html).toContain('href="/experiments"');expect(html).toContain('href="/research"');expect(html).not.toContain("local preview")});it("offers one obvious beginner starting action",()=>{const html=renderToStaticMarkup(<DashboardSummary snapshot={createDemoSnapshot("o1")}/>);expect(html).toContain('href="/plants/new"');expect(html).toContain("เริ่มจากต้นไม้ 1 ต้น")})});
