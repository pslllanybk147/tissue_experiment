"use client";

import { useState, type FormEvent } from "react";
import type { EvidenceState, ProtocolDraftInput, ProtocolStep } from "../../lib/domain/models";
import { validateProtocolDraft } from "../../lib/domain/protocol-validation";

export function ProtocolEditor({ initialValue, onSubmit }: { initialValue: ProtocolDraftInput; onSubmit: (value: ProtocolDraftInput) => Promise<void> }) {
  const [value, setValue] = useState(initialValue); const [error, setError] = useState(""); const [pending, setPending] = useState(false);
  const updateStep = (index: number, patch: Partial<ProtocolStep>) => setValue(current => ({ ...current, steps: current.steps.map((step, i) => i === index ? { ...step, ...patch } : step) }));
  const move = (index: number, delta: number) => setValue(current => { const steps = [...current.steps]; const target = index + delta; if (target < 0 || target >= steps.length) return current; [steps[index], steps[target]] = [steps[target], steps[index]]; return { ...current, steps: steps.map((step, order) => ({ ...step, order })) }; });
  const add = () => setValue(current => ({ ...current, steps: [...current.steps, { id: crypto.randomUUID(), order: current.steps.length, title: "", instruction: "", durationMinutes: null, criticalControls: [], safetyNotes: [], referenceIds: [], evidenceState: current.evidenceState }] }));
  async function submit(event: FormEvent) { event.preventDefault(); const errors = validateProtocolDraft(value); if (Object.keys(errors).length) return setError(Object.values(errors).filter(v => typeof v === "string").join(" · ")); setError(""); setPending(true); try { await onSubmit(value); } catch (cause) { setError(cause instanceof Error ? cause.message : "บันทึก Protocol ไม่สำเร็จ"); } finally { setPending(false); } }
  return <form className="protocol-editor experiment-surface" onSubmit={submit}><div className="form-heading"><p className="eyebrow">PROTOCOL DRAFT</p><h1>Protocol editor</h1></div>
    {error && <p className="form-alert" role="alert">{error}</p>}
    <div className="form-grid"><label className="form-field"><span>ชื่อ Protocol</span><input value={value.title} onChange={e => setValue({ ...value, title: e.target.value })} /></label><label className="form-field"><span>ขอบเขตพืช</span><input value={value.plantScope} onChange={e => setValue({ ...value, plantScope: e.target.value })} /></label><label className="form-field"><span>Evidence</span><select value={value.evidenceState} onChange={e => setValue({ ...value, evidenceState: e.target.value as EvidenceState })}><option>Verified</option><option>Adapted</option><option>Experimental</option><option>Pending review</option></select></label><label className="form-field"><span>สรุป</span><textarea value={value.summary} onChange={e => setValue({ ...value, summary: e.target.value })} /></label><label className="form-field"><span>บันทึกเวอร์ชัน</span><input value={value.changeNote} onChange={e => setValue({ ...value, changeNote: e.target.value })} /></label></div>
    <div className="protocol-step-heading"><h2>ขั้นตอน</h2><button type="button" className="quiet-button" onClick={add}>เพิ่มขั้นตอน</button></div>
    {value.steps.map((step, index) => <section className="protocol-step-editor" key={step.id}><strong>ขั้นที่ {index + 1}</strong><input aria-label={`ชื่อขั้นตอน ${index + 1}`} placeholder="ชื่อขั้นตอน" value={step.title} onChange={e => updateStep(index, { title: e.target.value })} /><textarea aria-label={`คำสั่ง ${index + 1}`} placeholder="คำสั่งปฏิบัติ" value={step.instruction} onChange={e => updateStep(index, { instruction: e.target.value })} /><div className="step-controls"><button disabled={index === 0} type="button" onClick={() => move(index, -1)}>ขึ้น</button><button disabled={index === value.steps.length - 1} type="button" onClick={() => move(index, 1)}>ลง</button><button type="button" onClick={() => setValue(current => ({ ...current, steps: current.steps.filter((_, i) => i !== index).map((item, order) => ({ ...item, order })) }))}>ลบ</button></div></section>)}
    <div className="form-actions"><button className="primary-button" disabled={pending} type="submit">{pending ? "กำลังบันทึก…" : "บันทึกร่าง"}</button></div></form>;
}
