"use client";import Image from"next/image";import type{ObservationMedia}from"../../lib/domain/models";export function MediaStrip({items,onDelete}:{items:ObservationMedia[];onDelete:(id:string)=>Promise<void>}){if(!items.length)return null;return<div className="media-strip">{items.map(item=><figure key={item.id}><Image alt={item.caption||"ภาพ observation"} height={item.height} src={item.secureUrl} unoptimized width={item.width}/>{item.caption&&<figcaption>{item.caption}</figcaption>}<button type="button" onClick={()=>void onDelete(item.id)}>ลบรูป</button></figure>)}</div>}
