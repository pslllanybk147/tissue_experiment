import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it}from"vitest";import{MediaStrip}from"./media-strip";describe("MediaStrip",()=>{it("renders image and caption",()=>{const html=renderToStaticMarkup(<MediaStrip items={[{id:"m1",ownerId:"o",lotId:"l",observationId:"x",cloudinaryPublicId:"p",secureUrl:"https://example.com/a.jpg",width:10,height:10,format:"jpg",bytes:1,caption:"ยอดใหม่",capturedAt:null,createdBy:"o",createdAt:"t",updatedAt:"t",deletedAt:null}]} onDelete={async()=>{}}/>);expect(html).toContain("ยอดใหม่");expect(html).toContain("img")})});
