import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it}from"vitest";import{MediaUploader}from"./media-uploader";describe("MediaUploader",()=>{it("renders accepted formats and caption",()=>{const html=renderToStaticMarkup(<MediaUploader lotId="l1" observationId="x1" onUploaded={async()=>{}}/>);expect(html).toContain("image/jpeg,image/png,image/webp");expect(html).toContain("คำอธิบายภาพ")})});
