import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it}from"vitest";import{MediaUploader,readApiError}from"./media-uploader";describe("MediaUploader",()=>{it("renders accepted formats and caption",()=>{const html=renderToStaticMarkup(<MediaUploader lotId="l1" observationId="x1" onUploaded={async()=>{}}/>);expect(html).toContain("image/jpeg,image/png,image/webp");expect(html).toContain("คำอธิบายภาพ")});it("surfaces safe API errors for deployment diagnosis",async()=>{const response=new Response(JSON.stringify({error:"Invalid authentication"}),{status:401,headers:{"content-type":"application/json"}});await expect(readApiError(response,"fallback")).resolves.toBe("Invalid authentication")})});
